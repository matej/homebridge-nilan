{
  "pluginAlias": "Nilan",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "devices": {
      "type": "array",
      "items": {
        "title": "Device",
        "type": "object",
        "properties": {
          "name": {
            "title": "Name",
            "type": "string",
            "required": true,
            "default": "Compact P"
          },
          "host": {
            "title": "IP Address",
            "type": "string",
            "required": true,
            "format": "ipv4",
            "default": "192.168.5.107",
            "description": "Check the control panel in Super user mode under Settings -> Network Settings. Make sure you can ping the Nilan unit from your Homebridge device."
          },
          "schedule": {
            "title": "Read week schedule",
            "type": "boolean",
            "default": true,
            "description": "Enable this when a week schedule is programmed on the controller. In automatic mode the plugin reads HomeKit target values from the active schedule entry without writing them back to the controller."
          }
        }
      }
    }

  }
}
